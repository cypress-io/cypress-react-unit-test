/** @jsx jsx */
import { jsx } from "@emotion/core";

export const Emotion = props => {
  return (
    <div
      css={{
        color: "hotpink"
      }}
    >
      The Next Generation of CSS-in-JS
    </div>
  );
};
